# Gemma 26B (A4B MoE) — 로컬 프로덕션과 동일 모델.
# 이름은 'AWQ-4bit' 이지만 실제 quant_method 는 compressed-tensors(int4 pack-quantized).
# 공개 리포(커뮤니티 재업로드)라 보통 HF_TOKEN 없이 받아짐.
MODEL="cyankiwi/gemma-4-26B-A4B-it-AWQ-4bit"
SERVED_NAME="gemma-26b"
# 모델별 추가 vLLM 인자 (bash 배열). 예: MODEL_EXTRA_ARGS=(--quantization compressed-tensors)
MODEL_EXTRA_ARGS=()
